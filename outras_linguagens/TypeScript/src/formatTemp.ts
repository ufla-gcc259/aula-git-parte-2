// Imprime uma temperatura 'n' em Fahrenheit no formato n°F
export function formatFahrenheit(fahrenheit: number) {
  return `${fahrenheit}°F`;
}

// Imprime uma temperatura 'n' em Celsius no formato n°C
export function formatCelsius(celsius: number) {
  return `${celsius}°C`;
}
